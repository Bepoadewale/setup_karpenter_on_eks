apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: html-app-monitor
spec:
  type: LoadBalancer
  selector:
    app: prometheus
  ports:
    - port: 80
      targetPort: 9090
      protocol: TCP 